# Enable JWT and internal backends
# auth_backends.1 = rabbitmq_auth_backend_oauth2
# auth_backends.2 = internal
# # RabbitMQ will fetch this automatically and cache it
# jwt.signing_keys.jwks_url = http://localhost:8080/realms/myrealm/protocol/openid-connect/certs
# # Optional: cache expiry time (default: 60 seconds)
# jwt.signing_keys.refresh_interval =1h
# jwt.verify_claims = true
# jwt.expected_issuer = http://localhost:8080/realms/myrealm
# jwt.expected_audience = rabbitmq
# jwt.identity_claim = sub
# jwt.allowed_audiences.1 = rabbitmq

# auth_backends.1 = rabbitmq_auth_backend_ldap
# auth_backends.2 = internal
# ldap.servers = openldap
# ldap.port = 389
# ldap.user_dn_pattern = "uid=${username},ou=users,dc=example,dc=com"
# ldap.vhost_pattern = "/"
# ldap.tcp_timeout = 5000

